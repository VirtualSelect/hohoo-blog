# 对话记忆实验室与研究共创 · 2026-09-22

分支：codex/research-memory-lab。未提交、合并或发布。

## 实现

- 复用 ConversationWorkbench，替换原固定教学示例为作者三轮真实控制台摘录，支持逐轮请求结构回放。
- 裁剪模式可选择全部、最近一轮、无历史或逐条选择；始终保留当前问题，检查助手重复事实的情况。仅修改请求，不执行模型或预测答案。
- 导出 v1 JSON 封套标记 not-executed，不含凭证；请求结构基于控制台还原，不冒充原始抓包。
- 项目页直接体验；首页作品区增加深链；教程保留按需展开。修复 TryIt 深链未展开外层 details 的问题。
- docs/RESEARCH-COCREATION.md 列出三方向任务、证据、分工和验收，链接既有提案避免复制。没有新增假文章、成果或数据。

## 验证

48 项测试通过，包括回放不泄漏后续回答、裁剪保序与助手事实保留。生产构建通过，166 个静态页面；147 个路由检查无错误。修改文件格式检查通过。

浏览器检查：回放/裁剪切换、选择消息、JSON 预览、键盘 Enter、三语言、两主题，以及 1440/1280/1024/768/430/375 项目页面宽度；无横向溢出。已查看桌面夜间、手机护眼截图。

## 局限

没有调用真实 API 运行新对照。研究案例待作者共同执行。导出按钮已实现，但内置浏览器未返回下载完成事件，未确认文件落盘；JSON 可在页面直接查看。未知地址路由测试仍产生已有 Next.js NoFallbackError 服务端日志，响应仍为预期 404。

新增依赖：None。原有无关锁文件改动保留。
