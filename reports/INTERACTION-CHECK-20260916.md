# 2026-09-16 交互回归

## 发现与处理

- 原主题按钮使用单个 Unicode 字符，实测宽约 28px；改为 SVG + 护眼 / 夜间文字，至少 44px 点击区域。保留原存储键，并校验启动时的主题值；跨标签页同步偏好，存储失败时仍能切换当前页面。
- 动态路由响应前没有反馈；共用 Link 接入 Next.js `useLinkStatus`，等待时显示状态，提示不拦截点击，不锁定其他导航。
- 首页同步引入文章和 Radar 组件；改成动态加载，保留 SSR。服务端读取内容以 React `cache` 在单次渲染中复用，避免元数据与页面重复读取同一 JSON，不跨请求缓存开发内容。
- About 粒子原本随屏幕刷新率绘制；限制约 30fps、DPR 1.5，调整对应的慢帧降级阈值。保留暂停、复位、离屏与后台暂停。

## 验证

- `npm run test:web`：9 项通过；`node --test tests/*.test.*`：48 项通过。
- `npm run build` 成功。
- 独立生产预览 4182 的 `test:routes`：123 页面，兼容重定向、RSS 与 404 检查无错误。
- 浏览器：主题点击、键盘空格切换、刷新保留；Ctrl+K、Esc、关闭后搜索按钮焦点；手机菜单展开及跳转后关闭、回页首；学习完成状态；Radar 空搜索及清除、收藏进入阅读清单；粒子暂停、播放、拨散、复位；手机文章目录、代码复制。
- 首页导航在 375 / 430 / 768 / 1024 / 1280 / 1440 宽度无横向溢出；375px 文章及关于页可操作。检查浅色、深色、简体、繁体入口。
- 本次检查的页面无浏览器 error / warning；测试收藏已取消。

## 边界

- 未复现持续性全站按钮失效，不能将所有偶发无响应归因于同一个问题；以上为已验证的入口、反馈及渲染负担改进。
- 开发模式首次访问仍可能等待按需编译；未宣称已经消除冷启动，也未虚构性能提升百分比。
- 无新增依赖；未提交、推送或部署。保留用户已有的锁文件变更。
