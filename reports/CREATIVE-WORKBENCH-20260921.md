# 创意工作台 · 本地预览

- 分支：`codex/creative-workbench-20260921`。未推送、未合并、未发布。
- 预览：`http://localhost:4197/build#workbench`。
- 新增三种本地教学工具：决策调音台、信任边界剧场、JSON 结构透镜。沿用三个主学习方向，无新一级导航，无新依赖。
- 首页低权重“动手玩”链接到实践页；浅蓝、淡紫、杏色分区，深浅主题同步。
- Jev 参考：https://docs.typesafe.ai/introduction 。仅为概念教学，未调用 Jev，不提供真实模型概率或准确率。
- 新增安全响应头：nosniff、strict-origin-when-cross-origin、SAMEORIGIN、禁用摄像头/麦克风/定位。不阻止同源作品预览。不代表全面安全审计。

## 验证

- 45 项 Web 测试通过；新增权限优先级、阈值边界、无效参数、JSON 错误、输入大小、节点上限与特殊键测试。
- 生产构建通过；141 个页面路由检查通过；新增组件与配置格式检查通过。
- 浏览器验证置信度键盘调节、撤销授权仍阻止执行、教学答案反馈、JSON 解析与文本转义、三语言切换。
- 实践页 1440 / 1280 / 1024 / 768 / 430 / 375 屏宽未横向溢出；深浅主题已查看。手机复选框对齐问题已修正。
- 浏览器检查未捕获警告或错误。路由测试的未知地址沿用既有 Next.js NoFallbackError 日志，HTTP 404 返回正常；本轮未修改路由机制。

## 边界

- 教学工具无网络提交、无持久化、无模型后端。不执行输入、不作真实安全判断。
- JSON 仅检查语法与结构，不验证业务 Schema；最多展示 100 节点和 8 层。
- 用户已有的根目录锁文件改动未纳入本轮。
- 等用户确认预览后，再决定继续修改或提交合并。
