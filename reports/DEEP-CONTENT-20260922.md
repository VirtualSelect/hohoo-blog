# 三方向深度内容与阅读分类（已确认发布）

分支：`codex/deep-content-cases`。基于主干 `558e0a9`。2026-09-22 作者已确认合并主干并发布；生产状态以对应提交的部署结果为准。

## 已落地

- AI 应用：`/docs/ai-apps/radar-publishing-pipeline`。使用本站固定提交解释采集与发布边界、UTC 配额、相关性规则、去重/聚类区别，以及绿色任务却没有页面更新的证据排查顺序。
- LLM：`/docs/llm/kv-cache`。区分聊天记录、KV Cache、跨请求前缀复用；解释因果注意力与推理阶段；在明确假设下计算内存占用，并讨论 GQA 和优化限制。
- 具身智能：`/docs/embodied-ai/openvla-action-pipeline`。追踪原版 OpenVLA 输入、嵌入、动作 Token 与反归一化，说明边界数/中点数差异、数据统计量与控制器契约。
- 三篇各有英文、繁体中文译文和源文指纹。AI 译文未标记为人工审校；正文明确 AI 辅助整理与证据范围。
- 文章页增加阅读目的 `kind` 和研究方向 `domain` 筛选，支持分享、刷新、前进/后退、清空及无结果状态。保留 `type` 旧查询。
- 分类统一进入文章列表、专题列表和正文头部。AI Radar 案例位于 AI Applications 的 AI Engineering 下，博客项目也关联这篇案例。
- 修正博客项目中仍声称 Next.js 正待迁移的过期概述。

## 内容与范围边界

- 没有新增学习主线、虚构已完成 Lab、Token/性能指标或个人观点。
- 新文是完整可阅读的工程/机制文章，不计入已完成实验；未冒充模型或机器人实测。
- `published` 控制构建内的可见性；本批内容已获作者发布确认，未将技术导读升级为实测结论。
- 现有 `docs/RESEARCH-COCREATION.md` 的 A1/L1/E1 待办继续有效，没有重复增加任务；本次不调用付费 API、不下载模型或启动仿真。
- 未新增依赖。正文沿用现有服务端 Markdown、TOC、复制、关联阅读、主题和响应式规则。

## 验证

- `npm run test:web`：62 项通过，新增测试覆盖三语言文章可发现性/关系、所有印刷算例实际执行、组合筛选及旧链接兼容。
- 内容索引、学习索引与国际化测试：10 项通过。
- `npm run i18n:check`：无过期翻译提示。
- `npm run build`：178 个静态生成页面成功；不存在独立 lint/typecheck 脚本。
- 路由检查：159 条页面路由，加旧地址跳转、RSS、404 检查，无错误。
- Chrome：1440/1280/1024/768/430/375 宽度下检查文章页与三篇新文，护眼/夜间共 48 组，无横向页面溢出或控制台错误。
- 筛选后刷新、返回、空结果与清空均验证通过；三语言新页面返回 200。
- 最终预览使用 `http://localhost:4201/articles`；构建后重启预览服务，避免旧进程读取新构建文件。最终 Chrome 检查通过键盘筛选、切换语言保留筛选、专题跳转、Ctrl+K 搜索与正文无 JavaScript 阅读，canonical 正确。

## 后续共创

需要新的原创实验结果时，执行既有 A1/L1/E1 的一项，保留请求/输出或环境记录；不要把本文教学算例升级成研究结论。
