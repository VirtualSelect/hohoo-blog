# 未使用内容清理（2026-09-13）

## 判断依据

核对 Next.js 路由、Views 页面映射、内容构建脚本及仓库引用。目录名称不是删除依据：`blog/archive/` 中《一个新的开始》仍会发布，`src/pages/news/daily/` 仍用于日报。

## 已移除

- `archive/legacy/`：7 份已退出发布的 Layui、MyBatis-Plus-Join、Npm 旧笔记及资源汇总、模板示例；Git 历史保留。
- 5 份旧框架专用翻译配置：简体插件及主题配置、英文 Blog options；当前翻译仍使用各语言 `code.json` 和正文文件。
- 三个 Topic 的 `_category_.json`：当前 Topic 来自数据模型，不读取旧侧栏配置。
- `aboutMe`、`lab`、`news`、`news/weekly` 旧页面及 `LegacyRedirect`：已由 Next.js catch-all 路由的服务端重定向接管，旧 URL 继续兼容。

## 保留

已发布教程和随笔及其翻译、资讯与日报、项目与实验数据、计划节点和内容模板、审核记录、历史验收报告、仍在使用的共享页面与粒子实验应用。历史报告描述当时状态，不当作当前运行配置。

未跟踪的 `pnpm-lock.yaml`、`yarn.lock` 不属于本次确认的历史文件，未改动。

## 验证

- 根目录 43 项测试及 Web 三语内容 6 项测试通过。
- 生产构建通过，生成 130 个页面。
- 在 `apps/web` 工作目录启动生产服务后，现有路由脚本验证 111 个内容页面、三语旧地址跳转、RSS 和 404，错误为 0。
- 本轮未修改页面样式和交互；未进行新的视觉验收。
